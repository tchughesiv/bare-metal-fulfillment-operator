---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
    controller-gen.kubebuilder.io/version: v0.18.0
  name: baremetalpools.osac.openshift.io
spec:
  group: osac.openshift.io
  names:
    kind: BareMetalPool
    listKind: BareMetalPoolList
    plural: baremetalpools
    shortNames:
    - bmp
    - bmpool
    singular: baremetalpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.conditions[?(@.type == 'Ready')].reason
      name: Reason
      type: string
    - jsonPath: .spec.profile.name
      name: Profile
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BareMetalPool is the Schema for the baremetalpools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BareMetalPoolSpec defines the desired state of BareMetalPool.
            properties:
              hostSets:
                description: HostSets defines the number of hosts needed for each
                  host class.
                items:
                  properties:
                    hostType:
                      description: HostType specifies the type of the host
                      minLength: 1
                      type: string
                    replicas:
                      description: Replicas specifies the number of hosts required
                        for this host class
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - hostType
                  - replicas
                  type: object
                  x-kubernetes-validations:
                  - rule: self.replicas > 0
                type: array
                x-kubernetes-list-map-keys:
                - hostType
                x-kubernetes-list-type: map
              profile:
                description: |-
                  Profile specifies configuration for the bare metal pool, such as setup jobs
                  and setting additional metadata labels
                properties:
                  name:
                    description: Name is the name of the profile
                    minLength: 1
                    type: string
                  templateParameters:
                    description: |-
                      TemplateParameters is a JSON-encoded map of the parameter values for the
                      selected profile.
                    type: string
                required:
                - name
                type: object
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
            required:
            - hostSets
            type: object
          status:
            description: BareMetalPoolStatus defines the observed state of BareMetalPool.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the BareMetalPool state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              desiredConfigVersion:
                description: DesiredConfigVersion is a hash of the spec, used to detect
                  spec changes and control retry behavior.
                type: string
              hostSets:
                description: HostSets shows the current allocation of hosts
                items:
                  properties:
                    hostType:
                      description: HostType specifies the type of the host
                      minLength: 1
                      type: string
                    replicas:
                      description: Replicas specifies the number of hosts required
                        for this host class
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - hostType
                  - replicas
                  type: object
                type: array
              jobs:
                description: |-
                  Jobs tracks the history of provision and deprovision operations
                  Ordered chronologically, with latest operations at the end
                  Limited to the last N jobs (configurable via OSAC_MAX_JOB_HISTORY, default 10)
                items:
                  description: JobStatus represents the status of a provisioning or
                    deprovisioning job
                  properties:
                    blockDeletionOnFailure:
                      description: |-
                        BlockDeletionOnFailure indicates whether CR deletion should be blocked if this job fails
                        AAP Direct sets this to true to prevent orphaned cloud resources
                        EDA sets this to false as webhook handles cleanup
                      type: boolean
                    configVersion:
                      description: |-
                        ConfigVersion is the DesiredConfigVersion at the time this job was triggered.
                        Used to determine retry behavior on failure: if ConfigVersion differs from
                        the current DesiredConfigVersion, a new job is triggered immediately.
                        If they match, the controller retries with exponential backoff.
                      type: string
                    jobID:
                      description: |-
                        JobID is the job identifier from the provisioning provider
                        For AAP Direct: job ID from AAP API response
                        For EDA: auto-incremented "eda-webhook-N"
                      type: string
                    message:
                      description: Message provides human-readable status or error
                        information
                      type: string
                    state:
                      description: State is the current state of the job
                      enum:
                      - Pending
                      - Waiting
                      - Running
                      - Succeeded
                      - Failed
                      - Canceled
                      - Unknown
                      type: string
                    timestamp:
                      description: Timestamp when this job was created/triggered
                      format: date-time
                      type: string
                    type:
                      description: Type indicates the operation type
                      enum:
                      - provision
                      - deprovision
                      - attach
                      - detach
                      type: string
                  required:
                  - jobID
                  - state
                  - timestamp
                  - type
                  type: object
                type: array
              lastUpdated:
                description: LastUpdated is the timestamp when the status was last
                  updated
                format: date-time
                type: string
              phase:
                description: Phase provides a single-value overview of the state of
                  the BareMetalPool
                enum:
                - Progressing
                - Failed
                - Ready
                - Deleting
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
